# Copy this file and rename it to .env
# Paste your OpenAI API key below (get one at platform.openai.com/api-keys)
# The .env file is gitignored so your key won't be pushed to GitHub
OPENAI_API_KEY=your_api_key_here
